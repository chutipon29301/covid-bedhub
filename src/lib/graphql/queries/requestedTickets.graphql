query RequestedTicket($data: RequestTicketQueryDto) {
	requestedTicket(data: $data) {
		tickets {
			id
			patient {
				firstName
				lastName
				sex
				age
			}
			createdAt
			riskLevel
			status
		}
		count
	}
}
